{
  "name": "strprox-wasm",
  "version": "0.1.0",
  "description": "Port strprox to wasm",
  "private": true,
  "type": "module",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "build:move" : "rm pkg/package* && cp pkg/* publish",
    "build:move_wasm": "cp publish/strprox_bg.wasm publish/dist/umd/strprox_bg.wasm",
    "build:umd": "npm run wasm-pack nodejs && rollup -c umd.rollup.config.js && npm run build:move && npm run build:move_wasm",
    "build:es": "npm run wasm-pack web && rollup -c es.rollup.config.js && npm run build:move",
    "build": "npm run build:umd && npm run build:es",
    "foo": "echo *.{js}",
    "clean": "rm -r pkg",
    "test": "echo \"Error: no test specified\" && exit 1",
    "wasm-pack": "wasm-pack build -d npm/strprox-wasm/pkg -t $1"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@rollup/plugin-wasm": "^6.2.2"
  }
}
